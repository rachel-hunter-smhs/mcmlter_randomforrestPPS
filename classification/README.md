This folder will contain outputs for each classification model.
Each subdirectory is named `<dataset>/<target>` and includes saved Random Forest models, importance tables, metrics, and diagnostic plots.
